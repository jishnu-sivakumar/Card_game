UNFINISHED

Worked with socket and tkinter to create a multiplayer(LAN/TCP) Card game


dependencies:
```bash
pip install customtkinter
pip install pillow
```

run 2 servers,
Init_server : for initialising all players and assigning their cards
game_server: for actual game interactions

prototype_ : main working game and logic file that can run the game on cli
init_client: talks with init server
prototype_round_client: placeholder round decider client whihc will be modified/merged later
frontend_custom : main GUI file based on customtkinter frontend
